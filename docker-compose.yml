services:
  livo-pg:
    image: pgvector/pgvector:pg17
    environment:
      - POSTGRES_USER=livo
      - POSTGRES_PASSWORD=livo
      - POSTGRES_DB=livo
    ports:
      - "5432:5432"
    volumes:
      - ./docker/setup.sql:/docker-entrypoint-initdb.d/setup.sql